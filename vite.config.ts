import { defineConfig } from 'vite'

export default defineConfig({
    base: "/worstcase-monaco-demo/",
    plugins: [],
    build: {
        rollupOptions: {
            output: {
            }
        }
    }
})
